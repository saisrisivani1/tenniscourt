streamlit               # Latest stable version of Streamlit
torch                    # PyTorch for YOLOv5
ultralytics                      # YOLOv5 package (if using ultralytics' library)
opencv-python-headless           # OpenCV without GUI support
numpy                            # Numerical operations
pandas                           # Data manipulation, if needed
Pillow                           # Image processing
matplotlib                       # Plotting, if needed
requests                         # For making HTTP requests, if needed
scipy                            # Scientific computing, if required

# Any additional dependencies your app specifically uses:
# Add other packages if your app relies on them
